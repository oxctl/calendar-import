{
  "name": "deployment",
  "version": "1.0.0",
  "scripts": {
    "install-browsers": "npx playwright install",
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.27.1",
    "dotenv": "^16.0.3"
  }
}
