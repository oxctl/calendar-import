SKIP_PREFLIGHT_CHECK=true

# We don't want a browser as we need to be started from a LTI launch
BROWSER=none

# The endpoint that handles the LTI launch (we get the JWT from this endpoint)
VITE_SERVER_LTI=https://lti-dev.canvas.ox.ac.uk
# The endpoint uses to proxy requests to Canvas and handles developer keys
VITE_SERVER_PROXY=https://proxy-dev.canvas.ox.ac.uk
# The calendar import server that keeps track of the imports and the performs new imports
VITE_SERVER_CALENDAR=https://calendar-import-dev.canvas.ox.ac.uk

